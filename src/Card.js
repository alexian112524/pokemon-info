import React, { useState, useCallback } from 'react';

const Card = (props) => {
    return (
        <div className="Card">
            <h3>Ceci est une carte</h3>
        </div>
    );
};

export default Card;